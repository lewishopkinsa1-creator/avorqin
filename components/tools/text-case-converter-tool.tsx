"use client";
import { useState } from "react";
import { toTitleCase,toCamelCase,toPascalCase,toSnakeCase,toKebabCase } from "@/lib/tool-utils/text-tool-utils";
export function TextCaseConverterTool(){const[input,setInput]=useState("Avorqin simple developer tools");const [mode,setMode]=useState("title");const output=mode==="upper"?input.toUpperCase():mode==="lower"?input.toLowerCase():mode==="camel"?toCamelCase(input):mode==="pascal"?toPascalCase(input):mode==="snake"?toSnakeCase(input):mode==="kebab"?toKebabCase(input):toTitleCase(input);return <div className="space-y-4"><textarea value={input} onChange={e=>setInput(e.target.value)} rows={8} className="w-full rounded-lg border p-3"/><select value={mode} onChange={e=>setMode(e.target.value)} className="rounded-lg border p-3"><option value="title">Title Case</option><option value="upper">UPPERCASE</option><option value="lower">lowercase</option><option value="camel">camelCase</option><option value="pascal">PascalCase</option><option value="snake">snake_case</option><option value="kebab">kebab-case</option></select><textarea value={output} readOnly rows={5} className="w-full rounded-lg border bg-slate-50 p-3"/></div>}