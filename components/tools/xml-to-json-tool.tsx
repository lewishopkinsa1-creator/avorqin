"use client";
import { useState } from "react";
import { xmlToJson } from "@/lib/tool-utils/xml-converter-utils";
export function XMLToJSONTool(){const[input,setInput]=useState("<user><name>Avorqin</name><active>true</active></user>");const[output,setOutput]=useState("");const[error,setError]=useState("");const run=()=>{try{setOutput(xmlToJson(input));setError("")}catch(e){setError(e instanceof Error?e.message:"Conversion failed");setOutput("")}};return <div className="space-y-4"><textarea value={input} onChange={e=>setInput(e.target.value)} rows={12} className="w-full rounded-lg border p-3 font-mono text-sm"/><button onClick={run} className="rounded-lg bg-slate-950 px-4 py-2 text-white">Convert to JSON</button>{error&&<p className="text-sm text-red-600">{error}</p>}<textarea value={output} readOnly rows={12} className="w-full rounded-lg border bg-slate-50 p-3 font-mono text-sm"/>{output&&<button onClick={()=>navigator.clipboard.writeText(output)} className="rounded-lg border px-4 py-2">Copy</button>}</div>}