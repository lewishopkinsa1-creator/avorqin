"use client";
import { useState } from "react";
import { encodeHtml } from "@/lib/tool-utils/html-entity-utils";
export function HTMLEncoderTool(){const[input,setInput]=useState('<div class="example">Avorqin & tools</div>');const output=encodeHtml(input);return <div className="space-y-4"><textarea value={input} onChange={e=>setInput(e.target.value)} rows={10} className="w-full rounded-lg border p-3 font-mono text-sm"/><textarea value={output} readOnly rows={10} className="w-full rounded-lg border bg-slate-50 p-3 font-mono text-sm"/><button onClick={()=>navigator.clipboard.writeText(output)} className="rounded-lg bg-slate-950 px-4 py-2 text-white">Copy Encoded HTML</button></div>}