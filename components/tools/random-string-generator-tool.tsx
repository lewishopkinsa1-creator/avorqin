"use client";
import { useState } from "react";
import { secureRandomString } from "@/lib/tool-utils/random-utils";
export function RandomStringGeneratorTool(){const[length,setLength]=useState(24),[output,setOutput]=useState(""),[symbols,setSymbols]=useState(false);const run=()=>setOutput(secureRandomString(length,{lower:true,upper:true,numbers:true,symbols}));return <div className="space-y-4"><input type="number" min={1} max={256} value={length} onChange={e=>setLength(Number(e.target.value))} className="rounded-lg border p-3"/><label className="flex gap-2"><input type="checkbox" checked={symbols} onChange={e=>setSymbols(e.target.checked)}/> Include symbols</label><button onClick={run} className="rounded-lg bg-slate-950 px-4 py-2 text-white">Generate String</button><input value={output} readOnly className="w-full rounded-lg border bg-slate-50 p-3 font-mono"/>{output&&<button onClick={()=>navigator.clipboard.writeText(output)} className="rounded-lg border px-4 py-2">Copy</button>}</div>}