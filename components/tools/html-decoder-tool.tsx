"use client";
import { useState } from "react";
import { decodeHtml } from "@/lib/tool-utils/html-entity-utils";
export function HTMLDecoderTool(){const[input,setInput]=useState("&lt;div&gt;Avorqin &amp; tools&lt;/div&gt;");const[output,setOutput]=useState("");return <div className="space-y-4"><textarea value={input} onChange={e=>setInput(e.target.value)} rows={10} className="w-full rounded-lg border p-3 font-mono text-sm"/><button onClick={()=>setOutput(decodeHtml(input))} className="rounded-lg bg-slate-950 px-4 py-2 text-white">Decode HTML</button><textarea value={output} readOnly rows={10} className="w-full rounded-lg border bg-slate-50 p-3 font-mono text-sm"/>{output&&<button onClick={()=>navigator.clipboard.writeText(output)} className="rounded-lg border px-4 py-2">Copy</button>}</div>}