"use client";
import { useState } from "react";
import { testRegex } from "@/lib/tool-utils/web-tool-utils";
export function RegexTesterTool(){const[pattern,setPattern]=useState("\\\\btool\\\\w*"),[flags,setFlags]=useState("gi"),[input,setInput]=useState("Tools are useful. This toolset is fast."),[output,setOutput]=useState(""),[error,setError]=useState("");const run=()=>{try{setOutput(JSON.stringify(testRegex(pattern,flags,input),null,2));setError("")}catch(e){setError(e instanceof Error?e.message:"Invalid regex");setOutput("")}};return <div className="space-y-4"><div className="grid gap-3 md:grid-cols-[1fr_120px]"><input value={pattern} onChange={e=>setPattern(e.target.value)} className="rounded-lg border p-3 font-mono"/><input value={flags} onChange={e=>setFlags(e.target.value)} className="rounded-lg border p-3 font-mono"/></div><textarea value={input} onChange={e=>setInput(e.target.value)} rows={8} className="w-full rounded-lg border p-3"/><button onClick={run} className="rounded-lg bg-slate-950 px-4 py-2 text-white">Test Regex</button>{error&&<p className="text-red-600 text-sm">{error}</p>}<textarea value={output} readOnly rows={10} className="w-full rounded-lg border bg-slate-50 p-3 font-mono text-sm"/></div>}